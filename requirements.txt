torch>=2.1
torchaudio>=2.1
einops>=0.7
einx>=0.3
packaging
pytorch-lightning>=2.2
hydra-core>=1.3
omegaconf
numpy
soundfile
tqdm
tensorboard
transformers>=4.51
# LLM-TTS component (Orpheus-style) — install when building/using it:
#   snac>=1.2          # SNAC 24 kHz neural codec
#   neucodec           # NeuCodec 24 kHz FSQ neural codec (alternative to SNAC)
#   datasets>=2.18     # for LLM training data loading
#   vllm>=0.5          # optional vLLM streaming inference backend (CUDA-only)
